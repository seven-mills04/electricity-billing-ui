# Production environment — used when running `npm run build`
# Leave VITE_API_URL commented out below if you want to set it in your Vercel/Netlify environment settings.
# VITE_API_URL=http://localhost:8080
